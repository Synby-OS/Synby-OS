modules:
  # install trivalent
  - type: dnf
    repos:
      cleanup: true
      files:
        - https://repo.secureblue.dev/secureblue.repo
    install:
      packages:
        - trivalent

  # apply selinux patches
  - type: script
    scripts:
      - selinux.sh
  - type: justfiles
    validate: true
    include:
      - utility.just
